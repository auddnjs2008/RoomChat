extends layouts/main
include mixins/user
block content
    .title
        h1 Room Chat
        <i class="fas fa-envelope-open"></i>
    .where
        if !loggedUser
            .loginButton
                button 
                    a(href="/login") Log In
                button
                    a(href="/join") Join
        else
            a.homeProfile(href=`/user/${loggedUser.id}/profile`)
                if loggedUser.avatarUrl
                    img.userPicture(src=loggedUser.avatarUrl)
                else
                    .homeImage
                       <i class="fas fa-user-alt"></i>
                .homeName=loggedUser.name
              



            .editProfielBtn.logoutButton
                button
                    a(href=`/user/${loggedUser.id}/editprofile`) Edit Profile

            .logoutButton
                button
                    a(href="/logout") Log Out        
    
    //- .homeAlarm
    //-     p |welcome to  Room Chat!!  This website is for  chatting and Notice Board.
    //-       | This is some explanation about  how to use this website
    //-       | 1. first  you  should Join  or Login(sometimes there is a login error, in situation that after you got a login there is a loading
    //-       |    try to push  F5 button  and  Home Icon.  After that you can't handle this login error, then please message to auddnjs2008@naver.com )
    //-       | 2. In People Icon , there is a friend List. you coulde add a friend in Search Icon by searching Email. 
    //-       | 3. Email Share Part In Notice Board ICon, If you press the Email Share Button, then  you share your email to all this website users. 