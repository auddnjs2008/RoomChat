extends layouts/main

block content
    .postEditWrapper
         .formContainer
            form(action=`/board/${post.id}/edit`,method="post")
                input(type="text", name="title" placeholder=post.title)
                textarea(name="content" value="Content")=post.content
                input(type="submit" value="Submit")